<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.basspro.bassprocommerce">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/bassproicon"
        android:label="@string/app_name"
        android:logo="@drawable/bassproicon"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Home" >

        </activity>
        <activity
            android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".activities.ReviewCart" />
        <activity android:name=".activities.FindAStore" />
        <activity android:name=".activities.ShopByDepartment" />
        <activity android:name=".activities.RewardsHome" />
        <activity android:name=".activities.LocalEvents" />
        <activity android:name=".activities.GiftCardPLP" />
        <activity android:name=".activities.ApplicationSettings" />
        <activity
            android:name=".activities.Login"
            android:label="@string/title_activity_login" />
        <activity android:name=".activities.Profile" />
        <activity android:name=".activities.PersonalInformation" />
        <activity android:name=".activities.PasswordManagement" />
        <activity android:name=".activities.SaveForLater" />
        <activity android:name=".activities.RewardsNewCard" />
        <activity android:name=".activities.RewardsNewCertificate" />
        <activity android:name=".activities.RewardsEnterReceipt" />
        <activity android:name=".activities.ProductListing" />
        <activity android:name=".activities.ProductDetail" />
        <activity android:name=".activities.Checkout" />
        <activity android:name=".activities.StoreInformation" />
        <activity android:name=".activities.CreditCardBalance" />
        <activity android:name=".activities.Offers" />
        <activity android:name=".activities.OfferDetails" />
        <activity android:name=".activities.ShopBySubDepartment" />
        <activity android:name=".activities.More" />
        <activity android:name=".activities.Register" />
        <activity android:name=".activities.Welcome" />
        <activity android:name=".activities.MyBarCodeActivity"></activity>
    </application>

</manifest>